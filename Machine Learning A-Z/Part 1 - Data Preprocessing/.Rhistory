dataset = read.csv('Data.csv')
df = read.csv('Data.csv')
df
df$Age = ifelse(df$Age = is.na(df$Age),
ave(df$Age, FUN = function(x) mean(x,na.rm = TRUE) ),
df$Age
)
df$Age = ifelse(df$Age = is.na(df$Age),
ave(df$Age, FUN = function(x) mean(x,na.rm = TRUE) ),
df$Age)
df$Age = ifelse(df$Age = is.na(df$Age), ave(df$Age, FUN = function(x) mean(x,na.rm = TRUE) ),df$Age)
df$Age = ifelse(is.na(df$Age), ave(df$Age, FUN = function(x) mean(x,na.rm = TRUE) ),df$Age)
df
df$Salary = ifelse(is.na(df$Salary), ave(df$Salary, FUN = function(x) mean(x,na.rm = TRUE) ),df$Salary)
df
airline=read.csv('C://myRepo//R//FL-Airline_Data//2008.csv')
airline$Origin
table(airline$Origin)
sort(table(airline$Origin))
head(airline$DepTime)
cut(airline$DepTime,breaks = seq(0,2400,by = 100) )
table(cut(airline$DepTime,breaks = seq(0,2400,by = 100) ))
plot(table(cut(airline$DepTime,breaks = seq(0,2400,by = 100) )))
